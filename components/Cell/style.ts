import { StyleSheet } from "react-native";

export default StyleSheet.create({
	container: {
		position: "absolute",
		flex: 1,
		borderRadius: 10,
		alignItems: "center",
		justifyContent: "center",
	},
});
